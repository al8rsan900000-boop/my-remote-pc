name: RemotePC
on: workflow_dispatch

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Set up RDP
      run: |
        echo "Preparing your PC..."
    - name: Setup Tmate
      uses: mxschmitt/action-tmate@v3
